{
  "rewrites": [
    { "source": "/api/(.*)", "destination": "https://neon-alpha1-production.up.railway.app/api/$1" },
    { "source": "/(.*)", "destination": "/index.html" }
  ],
  "buildCommand": "npm install && npm run build",
  "outputDirectory": "dist"
}
